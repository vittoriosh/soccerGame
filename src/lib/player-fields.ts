/** Columns the live draft board and player list actually render. */
export const PLAYER_CARD_SELECT = {
  id: true,
  name: true,
  overall: true,
  potential: true,
  age: true,
  positions: true,
  club: true,
  clubId: true,
  league: true,
  nationality: true,
  imageUrl: true,
} as const;
